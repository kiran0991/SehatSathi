import 'package:flutter/material.dart';

/// Shared color palette used across all SehatSathi screens.
class AppColors {
  static const Color primary = Color(0xFF064E3B);
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF0A0A0A);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color error = Color(0xFFDC2626);
  static const Color success = Color(0xFF16A34A);
  static const Color border = Color(0xFFD1D5DB);
  static const Color chipBackground = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);
}

/// Shared spacing, radius, and font tokens.
class AppStyles {
  static const String fontFamily = 'Inter';
  static const double cardRadius = 12.0;
  static const double inputRadius = 8.0;
  static const double pillRadius = 100.0;
}
